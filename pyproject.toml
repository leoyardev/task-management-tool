[project]
name = "task-management"
version = "0.1.0"
description = "Payback challenge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
